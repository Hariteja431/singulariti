import React from 'react';
import { WatermarkPdfClient } from './WatermarkPdfClient';

export const metadata = {
  title: 'Add Watermark to PDF Online Free | Singulariti',
  description: 'Add customized text or image watermarks to PDF pages locally in your browser. Fully custom positioning, colors, rotation, and opacity.',
};

export default function WatermarkPdfPage() {
  return <WatermarkPdfClient />;
}
