import React from 'react';
import { SignPdfClient } from './SignPdfClient';

export const metadata = {
  title: 'Sign PDF Online Free — Place Signature | Singulariti',
  description: 'Draw or upload a signature and place it on your PDF pages visually in your browser. Totally secure and private client-side tool.',
};

export default function SignPdfPage() {
  return <SignPdfClient />;
}
