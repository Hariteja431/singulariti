import React from 'react';
import { DeletePdfPagesClient } from './DeletePdfPagesClient';

export const metadata = {
  title: 'Delete PDF Pages Online Free — Remove Pages | Singulariti',
  description: 'Remove unwanted pages from a PDF document visually. Safe, client-side, browser-only file compiling.',
};

export default function DeletePdfPagesPage() {
  return <DeletePdfPagesClient />;
}
