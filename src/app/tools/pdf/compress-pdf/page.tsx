import React from 'react';
import { CompressPdfClient } from './CompressPdfClient';

export const metadata = {
  title: 'Compress PDF Online Free — Reduce PDF Size | Singulariti',
  description: 'Compress PDF files directly in your browser. Reduce document size locally with zero server file uploads.',
};

export default function CompressPdfPage() {
  return <CompressPdfClient />;
}
